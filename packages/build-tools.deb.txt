# Debian/Ubuntu build tools. libtool-bin: the `libtool` BINARY is split out on
# Debian/Ubuntu. autopoint: gettext autoreconf support. gcc/g++/gfortran: base bootstrap compiler.
# (perf is kernel-tied on Ubuntu and added best-effort by the Dockerfile, not here.)
bison
flex
make
autoconf
automake
libtool
libtool-bin
patch
perl
libperl-dev
tar
gzip
unzip
texinfo
gettext
autopoint
m4
swig
pkg-config
gcc
g++
gfortran
strace
environment-modules

# --- added from a prefer_system/system_requirement review ---
git
rsync
# (EXTERN.h is covered by libperl-dev above)
# hostname: recipes' build banners call it (CORAL PRE_BUILD_BANNER); not in the
# minimal base. Build tool only, not fingerprinted.
hostname
