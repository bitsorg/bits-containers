# EL build tools + bits runtime prereqs. GNU tar + gzip are REQUIRED for
# byte-reproducible package tarballs (build_template.sh --sort/--mtime, gzip -n).
# perf: Adaptyst system_requirement, checked INSIDE the container. texinfo/gettext/
# m4/autotools: alidist recipes are autotools-first. environment-modules + strace:
# `bits preload`. gcc/gcc-c++/gcc-gfortran: the BASE bootstrap compiler bits uses to
# build GCC-Toolchain (the stack's real compiler is built by bits, not the image).
bison
flex
make
autoconf
automake
libtool
patch
perl
perl-core
tar
gzip
unzip
texinfo
gettext-devel
m4
swig
pkgconf-pkg-config
gcc
gcc-c++
gcc-gfortran
strace
environment-modules
perf

# --- added from a prefer_system/system_requirement review ---
# git: recipes fetch from git at build time ('git' system_requirement).
# rsync: CMakeRecipe.Prepare + many recipes rsync the source copy.
# perl-devel: EXTERN.h etc. ('libperl' system_requirement; perl-core lacks EXTERN.h).
git
rsync
perl-devel
